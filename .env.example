# Development-only example values. Never commit a real secret.
LEX_BEARER_TOKENS={"replace-with-random-token":["example-project"]}
# Optional selector when both keys are set: direct or hosted.
LEX_DECISION_ADAPTER=
LEX_TYPESAFE_API_KEY=
LEX_OPENROUTER_API_KEY=
