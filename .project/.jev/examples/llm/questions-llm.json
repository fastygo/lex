{
    "generates_autoregressively": {
      "type": "noul",
      "instructions": "Do LLMs generate output by sampling the next token conditioned on tokens already produced?"
    },
    "can_guarantee_no_hallucination": {
      "type": "noul",
      "instructions": "Can an LLM guarantee that a fluent answer will never contain a fact unsupported by the prompt or the world?"
    },
    "temperature_zero_is_unique": {
      "type": "noul",
      "instructions": "Does setting temperature to 0 guarantee a unique, identical completion across every provider, seed, and batching setup?"
    },
    "what_llm_predicts": {
      "type": "choice",
      "instructions": "What does an LLM primarily predict at inference time?",
      "criteria": {
        "The next token in a sequence, conditioned on the tokens so far": "Autoregressive next-token prediction",
        "A calibrated distribution over an answer space the caller defined in advance": "A typed decision primitive, not a string",
        "A single global embedding of the whole document with no generation step": "An encoder-only classifier",
        "A verified proof that the reply is factually true": "A checker, not a language model"
      }
    },
    "what_rlhf_optimizes": {
      "type": "choice",
      "instructions": "What does RLHF (or a close preference variant) optimize an LLM toward?",
      "criteria": {
        "Answers that human raters prefer: helpful, fluent text": "Preference optimization after pretraining",
        "Calibrated probabilities over a caller-defined option set": "A decision-training objective, not RLHF",
        "Zero type errors and no string generation": "A non-generative interface",
        "Exact next-token log-likelihood on the pretraining corpus only": "The pretraining objective, not RLHF"
      }
    },
    "how_structured_output_is_obtained": {
      "type": "choice",
      "instructions": "How do typical structured-output or tool-calling wrappers get JSON from an LLM?",
      "criteria": {
        "They constrain or validate a generated string against a schema, then parse it": "The decision is recovered from text",
        "They sample a typed value in one parallel pass with no string to parse": "A non-generative decision model",
        "They look up the answer in a relational database and skip the model": "No generation at all",
        "They return only an emoji that the application maps to keys": "Not how structured-output APIs work"
      }
    },
    "prose_confidence_reliability": {
      "type": "score",
      "instructions": "How reliable is a confidence percentage that an LLM writes in prose as a calibrated probability?",
      "criteria": [
        "Unreliable — the number is more generated text, not a statistic from a defined answer distribution",
        "Sometimes useful if the prompt asks the model to be honest",
        "As reliable as a probability computed over caller-defined options"
      ]
    },
    "generation_vs_typed_decisions": {
      "type": "score",
      "instructions": "How similar is autoregressive next-token generation to producing typed decisions in a single parallel pass?",
      "criteria": [
        "Nothing in common: one emits a string token by token, the other returns values over a predefined answer space",
        "Slight conceptual overlap — both can be asked questions — but the inner workings differ",
        "Substantial overlap in both concept and execution",
        "Nearly the same, differing only in surface syntax",
        "Identical"
      ]
    },
    "llm_as_sole_destructive_gate": {
      "type": "score",
      "instructions": "How appropriate is it to let an LLM be the only gate on a destructive action such as a refund, transfer, or delete?",
      "criteria": [
        "Appropriate — fluent wording is enough authority to execute",
        "Acceptable if temperature is set to 0",
        "Poor — treat the model output as a proposal and apply an independent check or a human"
      ]
    }
  }
